// algoritmo que mostre os 20 primeiros numeros pares 
for(let numPar = 2; numPar <= 40; numPar++){
    if(numPar%2==0){
        console.log(`${numPar}`)
    }                                        
}